machine:
  environment:
    LEVEL: development
  node:
    version: 0.12.7
  timezone:
    America/Los_Angeles

dependencies:
  cache_directories:
    - node_modules
  override:
    - make

test:
  override:
    - make test

deployment:
  release:
    tag: /v\d+.\d+.\d+.*/
    commands:
      - ./circle-npm-login.sh
      - make deploy
